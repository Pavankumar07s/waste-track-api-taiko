{
    "builds": [
        {
            "src": "src",
            "use": "@vercel/node",
            "config": {
                "runtime": "nodejs"
            }
        }
    ],
    "routes": [
        {
            "src": "dist/src/**/*.js",
            "dest": "/api"
        }
    ]
}